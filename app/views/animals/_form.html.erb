<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :animal_name %>
  </div>

  <div class="field">
    <%= form.label :born_at %>
    <%= form.datetime_select :born_at, id: :animal_born_at %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :animal_type %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :animal_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
